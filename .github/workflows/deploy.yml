name: Deploy Production

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to deploy from main history
        required: false
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  SSM_START_TIMEOUT_SECONDS: "300"
  SSM_EXECUTION_TIMEOUT_SECONDS: "1200"
  SSM_POLL_INTERVAL_SECONDS: "15"

permissions:
  contents: read
  id-token: write

concurrency:
  group: nome-deploy-production
  cancel-in-progress: false

jobs:
  deploy-production:
    name: deploy-production
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout release revision
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ inputs.ref || github.sha }}
          fetch-depth: 0

      - name: Validate release revision
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor HEAD refs/remotes/origin/main; then
            echo "The selected revision is not in main history." >&2
            exit 1
          fi

      - name: Validate deployment configuration
        shell: bash
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          DEPLOY_S3_BUCKET: ${{ secrets.DEPLOY_S3_BUCKET }}
          DEPLOY_INSTANCE_ID: ${{ secrets.DEPLOY_INSTANCE_ID }}
          DEPLOY_TARGET_DIR: ${{ secrets.DEPLOY_TARGET_DIR }}
        run: |
          set -euo pipefail
          : "${AWS_REGION:?Set the AWS_REGION repository secret}"
          : "${AWS_DEPLOY_ROLE_ARN:?Set the AWS_DEPLOY_ROLE_ARN repository secret}"
          : "${DEPLOY_S3_BUCKET:?Set the DEPLOY_S3_BUCKET repository secret}"
          : "${DEPLOY_INSTANCE_ID:?Set the DEPLOY_INSTANCE_ID repository secret}"
          : "${DEPLOY_TARGET_DIR:?Set the DEPLOY_TARGET_DIR repository secret}"

      - name: Resolve release metadata
        id: release
        shell: bash
        run: |
          set -euo pipefail
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "archive=$RUNNER_TEMP/nome-release.tar.gz" >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6.2.1
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          mask-aws-account-id: true

      - name: Create release archive
        shell: bash
        run: |
          set -euo pipefail
          git archive --format=tar.gz --output "${{ steps.release.outputs.archive }}" HEAD

      - name: Upload release artifact
        id: artifact
        shell: bash
        env:
          RELEASE_SHA: ${{ steps.release.outputs.sha }}
        run: |
          set -euo pipefail
          key="releases/${RELEASE_SHA}.tar.gz"
          aws s3 cp --only-show-errors "${{ steps.release.outputs.archive }}" "s3://${{ secrets.DEPLOY_S3_BUCKET }}/${key}"
          echo "key=$key" >> "$GITHUB_OUTPUT"

      - name: Send production deploy command
        id: ssm
        shell: bash
        env:
          DEPLOY_TARGET_DIR: ${{ secrets.DEPLOY_TARGET_DIR }}
          DEPLOY_INSTANCE_ID: ${{ secrets.DEPLOY_INSTANCE_ID }}
          RELEASE_SHA: ${{ steps.release.outputs.sha }}
        run: |
          set -euo pipefail
          release_url=$(aws s3 presign "s3://${{ secrets.DEPLOY_S3_BUCKET }}/${{ steps.artifact.outputs.key }}" --expires-in 3600)
          echo "::add-mask::$release_url"

          cat > "$RUNNER_TEMP/remote-deploy.sh" <<'EOF'
          set -euo pipefail

          tmp_dir="/tmp/nome-deploy-${RELEASE_SHA}"
          archive_path="${tmp_dir}/release.tar.gz"
          release_dir="${tmp_dir}/release"

          rm -rf "$tmp_dir"
          mkdir -p "$release_dir"
          trap 'rm -rf "$tmp_dir"' EXIT

          ARCHIVE_PATH="$archive_path" RELEASE_URL="$RELEASE_URL" python3 - <<'PY'
          import os
          import urllib.request

          urllib.request.urlretrieve(os.environ["RELEASE_URL"], os.environ["ARCHIVE_PATH"])
          PY

          tar -xzf "$archive_path" -C "$release_dir"
          chmod +x "$release_dir/scripts/deploy_release.sh"

          sudo -u ubuntu -H env \
            TARGET_DIR="$DEPLOY_TARGET_DIR" \
            RELEASE_SHA="$RELEASE_SHA" \
            RELEASE_SOURCE_DIR="$release_dir" \
            bash "$release_dir/scripts/deploy_release.sh"
          EOF

          remote_script_b64=$(base64 < "$RUNNER_TEMP/remote-deploy.sh" | tr -d '\n')

          jq -n \
            --arg release_sha "$RELEASE_SHA" \
            --arg release_url "$release_url" \
            --arg target_dir "$DEPLOY_TARGET_DIR" \
            --arg remote_script_b64 "$remote_script_b64" \
            --arg execution_timeout "$SSM_EXECUTION_TIMEOUT_SECONDS" \
            '{
              commands: [
                ("export RELEASE_SHA=" + ($release_sha | @sh)),
                ("export RELEASE_URL=" + ($release_url | @sh)),
                ("export DEPLOY_TARGET_DIR=" + ($target_dir | @sh)),
                ("printf %s " + ($remote_script_b64 | @sh) + " | base64 -d > /tmp/run-nome-deploy.sh"),
                "bash /tmp/run-nome-deploy.sh"
              ],
              executionTimeout: [$execution_timeout]
            }' > "$RUNNER_TEMP/ssm-params.json"

          command_id=$(
            aws ssm send-command \
              --instance-ids "$DEPLOY_INSTANCE_ID" \
              --document-name "AWS-RunShellScript" \
              --comment "Deploy Nome ${RELEASE_SHA}" \
              --parameters "file://$RUNNER_TEMP/ssm-params.json" \
              --timeout-seconds "$SSM_START_TIMEOUT_SECONDS" \
              --query "Command.CommandId" \
              --output text
          )

          echo "command_id=$command_id" >> "$GITHUB_OUTPUT"

      - name: Wait for deploy completion
        shell: bash
        env:
          DEPLOY_INSTANCE_ID: ${{ secrets.DEPLOY_INSTANCE_ID }}
          COMMAND_ID: ${{ steps.ssm.outputs.command_id }}
        run: |
          set -euo pipefail

          deadline=$((SECONDS + SSM_START_TIMEOUT_SECONDS + SSM_EXECUTION_TIMEOUT_SECONDS + SSM_POLL_INTERVAL_SECONDS))
          while (( SECONDS < deadline )); do
            status=$(aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'Status' --output text 2>/dev/null || true)

            case "$status" in
              Success)
                aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'StandardOutputContent' --output text
                exit 0
                ;;
              Pending|InProgress|Delayed|"")
                sleep "$SSM_POLL_INTERVAL_SECONDS"
                ;;
              *)
                echo "Deploy failed with status: $status"
                echo "--- stdout ---"
                aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'StandardOutputContent' --output text || true
                echo "--- stderr ---"
                aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'StandardErrorContent' --output text || true
                exit 1
                ;;
            esac
          done

          echo "Deploy timed out waiting for SSM command completion; cancelling command."
          aws ssm cancel-command --command-id "$COMMAND_ID" --instance-ids "$DEPLOY_INSTANCE_ID" >/dev/null 2>&1 || true
          aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'StandardOutputContent' --output text || true
          aws ssm get-command-invocation --command-id "$COMMAND_ID" --instance-id "$DEPLOY_INSTANCE_ID" --query 'StandardErrorContent' --output text || true
          exit 1
